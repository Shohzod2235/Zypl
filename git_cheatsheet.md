mkdir — команда для создания папок
cd — смена директории
ls — просмотр списка файлов и папок
## Базовые команды Git
git init — создать новый репозиторий
git status — показать статус репозитория
git log — показать историю коммитов
